package sourceone.pages;
import sourceone.key.*;
public class AddTitle extends TitlePage {

    public AddTitle(Page p)throws Exception{
	super(p, "Pending Titles", "Title=0", "Add Title", 1);
    }
}
