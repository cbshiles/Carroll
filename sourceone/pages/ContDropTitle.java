package sourceone.pages;
import sourceone.key.*;
public class ContDropTitle extends ContTitle {

    public ContDropTitle(Page p)throws Exception{
	super(p, "Release Contract Titles", "Title=1 ORDER BY VIN", "Drop Title", 3);
    }
}
